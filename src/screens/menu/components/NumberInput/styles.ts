import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {flexDirection: 'row', alignItems: 'center'},
  textButton: {fontSize: 30},
  text: {
    fontSize: 32,
    textAlign: 'center',
  },
  textContainer: {
    minWidth: 110,
    height: 50,
    alignItems: 'center',
    justifyContent: 'center',
    paddingTop: 8,
  },
});
